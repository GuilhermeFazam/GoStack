import CreateAppointmentServicespec from './CreateAppointmentService';

describe('CreateAppointment', () => {
    it('shold be able to create a new appointment', () => {
        expect(1 + 2).toBe(3);
    });

    // it('Shold not be able to create tow appointments on the same time', () => {
    //     expect(1 + 2).toBe(3);
    // });
});
